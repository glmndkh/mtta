Даалгавар: Match editor-ийн dropdown-д “бүлгээс гарсан бүх тоглогч”-дыг бүрэн оруулж, админ хослолыг гараар оноодог болго
Зорилго

1/8, 1/4, 1/2, финалын Match Editor доторх Тоглогч 1 / Тоглогч 2 dropdown-ууд дээр Group stage-ээс шалгарсан бүх тоглогч (эсвэл seed-тэй баг) харагддаг байх. Админ аль ч тоглогчийг сонгож хэн хэнтэй тоглохыг гараар зааж хадгална.

Өгөгдөл

Эх сурвалж: qualifiedPlayers[] (эсвэл groupStandings[]-ээс top N-г шүүж бэлтгэсэн жагсаалт).

Талбарууд: { id, name, groupName, rank, seed }.

Нэмэлт төлөв: assignedMatchId (эдгээр тоглогчоос хэд нь аль матчид аль талд орсон байна гэдгийг танина).

UI/UX шаардлага

Dropdown нь searchable typeahead байх (нэрээр хайна).

Опшн бүр дээр нэр + (Group, Rank/Seed) хэлбэрээр шошго харуул.

Ж: “Оюунцэцэг Энхтуяа — Group A, Seed #1”

Хэрэв тоглогч аль хэдийн өөр матчид томилогдсон бол опшны ард “(already in Mx)” тэмдэглэгээ, саарал өнгөөр.

Хоёр талд нэг тоглогчийг давхар сонгохыг хоригло (сонгох үед шууд анхааруулга).

Clear товч (сонголтыг цэвэрлэх).

Dropdown дээр “Free select mode (гараар хослуулах)” toggle:

ON: бүх шалгарсан тоглогчийг харуулна.

OFF: зөвхөн системийн санал болгосон seed-ийн дагуу хязгаарласан жагсаалт.

Логик ба хязгаарлалт

Unique constraint:

Нэг тоглогч нэгэн зэрэг хоёр өөр матчид байж болохгүй.

Нэг матчид Тоглогч 1 ≠ Тоглогч 2.

Сонголт хийхэд:

State: matches[matchId].player1Id / player2Id шинэчлэгдэнэ.

Давхцал илэрвэл: toast + улаан helper text, хадгалалт хаагдана.

Auto-seed (сонголтоор): “Generate by seed” дарвал эхний хослолыг seed-ийн дүрмээр бөглөж өгөөд, админ цааш гараар override хийж чадна.

Хадгалалт ба баталгаажуулалт

“Хадгалах” дээр дарахад:

PATCH /matches/:id body: { player1Id, player2Id }

Backend-аас validation error ирвэл UI дээр харуулж сонголтыг буцааж сэргээ.

Хадгалсны дараа:

qualifiedPlayers.assignedMatchId талбаруудыг шинэчилж, бусад dropdown-д (already in …) badge-ууд шууд шинэчлэгдэнэ.

Эрх ба түгжээ

Match-д үр дүн (оноо) аль хэдийн баталгаажсан бол:

Dropdown-ууд disabled (🔒 “locked after result” chip).

Админ override хийх тусгай “Unlock for edit” (confirm modal-тай).

Acceptance criteria

Dropdown-ууд бүх шалгарсан тоглогчийг (нэр + group + seed) харуулна, хайлттай.

Давхар томилгоо хийх оролдлогод UI анхааруулж, хадгалалт зогсоно.

Аль нэг тоглогч өөр матчид орсон бол бусад dropdown-д already in тэмдэгтэй харагдана.

“Generate by seed” → автоматаар бөглөнө; дараа нь гараар өөрчилж хадгалж болно.

Оноо баталгаажсан матчид dropdown түгжигдсэн байна.

Амжилттай хадгалсны дараа бүх dropdown-ын төлөв realtime шинэчлэгдэнэ.

Edge cases

Тоглогчийг цэвэрлээд өөр хүн сонгоход өмнөх томилгоо суллагдана.

Lucky draw идэвхтэй үед random seat хийж бөглөсөн сонголтыг гараар өөрчилж болдог.

Хэрэв шалгарсан тоглогчдын тоо сондгой бол “BYE” сонголтыг зөвшөөр (one-click assign).